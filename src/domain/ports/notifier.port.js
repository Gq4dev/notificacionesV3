/**
 * @typedef {Object} NotifierPort
 * @property {(url:string, body:any, headers?:Record<string,string>)=>Promise<{status:number,data:any}>} postJson
 */
export const NotifierPort = {}; // marcador de tipo
